/* eslint-disable camelcase,@typescript-eslint/no-unused-vars */
// DO NOT EDIT THIS FILE. AUTO-GENERATED BY mobiletto-orm-typedef-gen

import { H3Event, H3Error } from "h3";
import { MobilettoOrmSyncError } from "mobiletto-orm-typedef";

export default defineEventHandler(async (event: H3Event) => {
  return await filterErrors(event, "privateConfig.delete", async (event: H3Event) => {
    return await requireAdminAccountObject(event, "privateConfig.delete", async (event: H3Event, session, account) => {
      const privateConfigRepo = privateConfigRepository();
      const id = event?.context?.params?.id;
      if (!id) throw notFound(privateConfigRepo.typeDef.idFieldName());
      try {
        const privateConfig_by_id = await privateConfigRepo.safeFindById(id);
        if (privateConfig_by_id) {

          return await privateConfigRepo.remove(privateConfig_by_id);
        }
      } catch (e) {
        if (e instanceof H3Error) throw e;
        if (e instanceof MobilettoOrmSyncError) {
          logger.warn(`privateConfig.delete: sync error: ${e}`);
          throw conflict();
        }
        logger.error(`privateConfig.delete: unexpected error: ${e}`);
        throw serverError();
      }
      throw notFound(id);
    });
  });
});
