/* eslint-disable camelcase,@typescript-eslint/no-unused-vars */
// DO NOT EDIT THIS FILE. AUTO-GENERATED BY mobiletto-orm-typedef-gen

import { H3Event, H3Error } from "h3";
import { MobilettoOrmSyncError } from "mobiletto-orm-typedef";

export default defineEventHandler(async (event: H3Event) => {
  return await filterErrors(event, "media.delete", async (event: H3Event) => {
    return await requireAdminAccountObject(event, "media.delete", async (event: H3Event, session, account) => {
      const mediaRepo = mediaRepository();
      const id = event?.context?.params?.id;
      if (!id) throw notFound(mediaRepo.typeDef.idFieldName());
      try {
        const media_by_id = await mediaRepo.safeFindById(id);
        if (media_by_id) {

          return await mediaRepo.remove(media_by_id);
        }
      } catch (e) {
        if (e instanceof H3Error) throw e;
        if (e instanceof MobilettoOrmSyncError) {
          logger.warn(`media.delete: sync error: ${e}`);
          throw conflict();
        }
        logger.error(`media.delete: unexpected error: ${e}`);
        throw serverError();
      }
      throw notFound(id);
    });
  });
});
