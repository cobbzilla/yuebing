/* eslint-disable @typescript-eslint/no-unused-vars */
// DO NOT EDIT THIS FILE. AUTO-GENERATED BY mobiletto-orm-typedef-gen

import { H3Event } from "h3";
import { MobilettoOrmValidationErrors } from "mobiletto-orm-typedef";
import { DestinationAssetType, DestinationAssetTypeDef } from "yuebing-model";

export default defineEventHandler(async (event: H3Event) => {
  return await filterErrors(event, "destinationAsset.create", async (event: H3Event) => {
    return await requireAdminAccountObject(event, "destinationAsset.create", async (event: H3Event, session, account) => {
      const destinationAssetRepo = destinationAssetRepository();
      const id = event?.context?.params?.id;
      if (!id) throw notFound(destinationAssetRepo.typeDef.idFieldName());
      const obj: DestinationAssetType = await readBody(event);
      if (!obj) {
        throw badRequest();
      }
      if (DestinationAssetTypeDef.id(obj) !== id) {
        const errs: MobilettoOrmValidationErrors = {};
        errs[destinationAssetRepo.typeDef.idFieldName()] = ["mismatch"];
        throw validationError(errs);
      } else {
        return await destinationAssetRepo.create(obj);
      }
    });
  });
});
