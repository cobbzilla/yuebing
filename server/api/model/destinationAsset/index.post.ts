/* eslint-disable @typescript-eslint/no-unused-vars */
// DO NOT EDIT THIS FILE. AUTO-GENERATED BY mobiletto-orm-typedef-gen

import { H3Event } from "h3";
import { DestinationAssetTypeDef } from "yuebing-model";
import { MobilettoOrmFindApiOpts } from "/storeHelper.ts";

export default defineEventHandler(async (event: H3Event) => {
  return await filterErrors(event, "destinationAsset.search", async (event: H3Event) => {
    return await requireAdminAccountObject(event, "destinationAsset.search", async (event: H3Event, session, account) => {
      const opts: MobilettoOrmFindApiOpts = (await readBody(event)) || {};
      if (typeof DestinationAssetTypeDef.apiConfig.search?.validate === "function") {
        const validated = await DestinationAssetTypeDef.apiConfig.search.validate(account, null, opts);
        if (validated === true || (typeof validated === "object" && Object.keys(validated).length === 0)) {
          // successfully validated
        } else if (typeof validated === "object" && Object.keys(validated).length > 0) {
          throw validationError(validated);
        } else {
          throw validationError({global: ["validation"]});
        }
      }
      const destinationAssetRepo = destinationAssetRepository();
      if (opts.field && opts.value) {
        return await destinationAssetRepo.safeFindBy(opts.field, opts.value, opts.opts || {});
      } else if (opts.textSearch && DestinationAssetTypeDef.textSearchFields.length > 0) {
        return await destinationAssetRepo.find({ predicate: (obj) => DestinationAssetTypeDef.textMatch(obj, opts.textSearch || "") });
      } else {
        return await destinationAssetRepo.find(opts.opts || {});
      }
    });
  });
});
