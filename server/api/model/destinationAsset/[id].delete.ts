/* eslint-disable camelcase,@typescript-eslint/no-unused-vars */
// DO NOT EDIT THIS FILE. AUTO-GENERATED BY mobiletto-orm-typedef-gen

import { H3Event, H3Error } from "h3";
import { MobilettoOrmSyncError } from "mobiletto-orm-typedef";

export default defineEventHandler(async (event: H3Event) => {
  return await filterErrors(event, "destinationAsset.delete", async (event: H3Event) => {
    return await requireAdminAccountObject(event, "destinationAsset.delete", async (event: H3Event, session, account) => {
      const destinationAssetRepo = destinationAssetRepository();
      const id = event?.context?.params?.id;
      if (!id) throw notFound(destinationAssetRepo.typeDef.idFieldName());
      try {
        const destinationAsset_by_id = await destinationAssetRepo.safeFindById(id);
        if (destinationAsset_by_id) {

          return await destinationAssetRepo.remove(destinationAsset_by_id);
        }
      } catch (e) {
        if (e instanceof H3Error) throw e;
        if (e instanceof MobilettoOrmSyncError) {
          logger.warn(`destinationAsset.delete: sync error: ${e}`);
          throw conflict();
        }
        logger.error(`destinationAsset.delete: unexpected error: ${e}`);
        throw serverError();
      }
      throw notFound(id);
    });
  });
});
