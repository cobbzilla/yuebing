/* eslint-disable camelcase,@typescript-eslint/no-unused-vars */
// DO NOT EDIT THIS FILE. AUTO-GENERATED BY mobiletto-orm-typedef-gen

import { H3Event, H3Error } from "h3";
import { MobilettoOrmSyncError } from "mobiletto-orm-typedef";

export default defineEventHandler(async (event: H3Event) => {
  return await filterErrors(event, "discoveredAsset.delete", async (event: H3Event) => {
    return await requireAdminAccountObject(event, "discoveredAsset.delete", async (event: H3Event, session, account) => {
      const discoveredAssetRepo = discoveredAssetRepository();
      const id = event?.context?.params?.id;
      if (!id) throw notFound(discoveredAssetRepo.typeDef.idFieldName());
      try {
        const discoveredAsset_by_id = await discoveredAssetRepo.safeFindById(id);
        if (discoveredAsset_by_id) {

          return await discoveredAssetRepo.remove(discoveredAsset_by_id);
        }
      } catch (e) {
        if (e instanceof H3Error) throw e;
        if (e instanceof MobilettoOrmSyncError) {
          logger.warn(`discoveredAsset.delete: sync error: ${e}`);
          throw conflict();
        }
        logger.error(`discoveredAsset.delete: unexpected error: ${e}`);
        throw serverError();
      }
      throw notFound(id);
    });
  });
});
