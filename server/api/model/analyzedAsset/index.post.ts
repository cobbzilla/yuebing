/* eslint-disable @typescript-eslint/no-unused-vars */
// DO NOT EDIT THIS FILE. AUTO-GENERATED BY mobiletto-orm-typedef-gen

import { H3Event } from "h3";
import { AnalyzedAssetTypeDef } from "yuebing-model";
import { MobilettoOrmFindApiOpts } from "/storeHelper.ts";

export default defineEventHandler(async (event: H3Event) => {
  return await filterErrors(event, "analyzedAsset.search", async (event: H3Event) => {
    return await requireAdminAccountObject(event, "analyzedAsset.search", async (event: H3Event, session, account) => {
      const opts: MobilettoOrmFindApiOpts = (await readBody(event)) || {};
      if (typeof AnalyzedAssetTypeDef.apiConfig.search?.validate === "function") {
        const validated = await AnalyzedAssetTypeDef.apiConfig.search.validate(account, null, opts);
        if (validated === true || (typeof validated === "object" && Object.keys(validated).length === 0)) {
          // successfully validated
        } else if (typeof validated === "object" && Object.keys(validated).length > 0) {
          throw validationError(validated);
        } else {
          throw validationError({global: ["validation"]});
        }
      }
      const analyzedAssetRepo = analyzedAssetRepository();
      if (opts.field && opts.value) {
        return await analyzedAssetRepo.safeFindBy(opts.field, opts.value, opts.opts || {});
      } else if (opts.textSearch && AnalyzedAssetTypeDef.textSearchFields.length > 0) {
        return await analyzedAssetRepo.find({ predicate: (obj) => AnalyzedAssetTypeDef.textMatch(obj, opts.textSearch || "") });
      } else {
        return await analyzedAssetRepo.find(opts.opts || {});
      }
    });
  });
});
