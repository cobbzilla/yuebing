/* eslint-disable @typescript-eslint/no-unused-vars */
// DO NOT EDIT THIS FILE. AUTO-GENERATED BY mobiletto-orm-typedef-gen

import { H3Event } from "h3";
import { MobilettoOrmValidationErrors } from "mobiletto-orm-typedef";
import { AnalyzedAssetType, AnalyzedAssetTypeDef } from "yuebing-model";

export default defineEventHandler(async (event: H3Event) => {
  return await filterErrors(event, "analyzedAsset.create", async (event: H3Event) => {
    return await requireAdminAccountObject(event, "analyzedAsset.create", async (event: H3Event, session, account) => {
      const analyzedAssetRepo = analyzedAssetRepository();
      const id = event?.context?.params?.id;
      if (!id) throw notFound(analyzedAssetRepo.typeDef.idFieldName());
      const obj: AnalyzedAssetType = await readBody(event);
      if (!obj) {
        throw badRequest();
      }
      if (AnalyzedAssetTypeDef.id(obj) !== id) {
        const errs: MobilettoOrmValidationErrors = {};
        errs[analyzedAssetRepo.typeDef.idFieldName()] = ["mismatch"];
        throw validationError(errs);
      } else {
        return await analyzedAssetRepo.create(obj);
      }
    });
  });
});
