/* eslint-disable camelcase,@typescript-eslint/no-unused-vars */
// DO NOT EDIT THIS FILE. AUTO-GENERATED BY mobiletto-orm-typedef-gen

import { H3Event, H3Error } from "h3";
import { MobilettoOrmSyncError } from "mobiletto-orm-typedef";

export default defineEventHandler(async (event: H3Event) => {
  return await filterErrors(event, "analyzedAsset.delete", async (event: H3Event) => {
    return await requireAdminAccountObject(event, "analyzedAsset.delete", async (event: H3Event, session, account) => {
      const analyzedAssetRepo = analyzedAssetRepository();
      const id = event?.context?.params?.id;
      if (!id) throw notFound(analyzedAssetRepo.typeDef.idFieldName());
      try {
        const analyzedAsset_by_id = await analyzedAssetRepo.safeFindById(id);
        if (analyzedAsset_by_id) {

          return await analyzedAssetRepo.remove(analyzedAsset_by_id);
        }
      } catch (e) {
        if (e instanceof H3Error) throw e;
        if (e instanceof MobilettoOrmSyncError) {
          logger.warn(`analyzedAsset.delete: sync error: ${e}`);
          throw conflict();
        }
        logger.error(`analyzedAsset.delete: unexpected error: ${e}`);
        throw serverError();
      }
      throw notFound(id);
    });
  });
});
