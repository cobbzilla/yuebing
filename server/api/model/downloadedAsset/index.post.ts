/* eslint-disable @typescript-eslint/no-unused-vars */
// DO NOT EDIT THIS FILE. AUTO-GENERATED BY mobiletto-orm-typedef-gen

import { H3Event } from "h3";
import { DownloadedAssetTypeDef } from "yuebing-model";
import { MobilettoOrmFindApiOpts } from "/storeHelper.ts";

export default defineEventHandler(async (event: H3Event) => {
  return await filterErrors(event, "downloadedAsset.search", async (event: H3Event) => {
    return await requireAdminAccountObject(event, "downloadedAsset.search", async (event: H3Event, session, account) => {
      const opts: MobilettoOrmFindApiOpts = (await readBody(event)) || {};
      if (typeof DownloadedAssetTypeDef.apiConfig.search?.validate === "function") {
        const validated = await DownloadedAssetTypeDef.apiConfig.search.validate(account, null, opts);
        if (validated === true || (typeof validated === "object" && Object.keys(validated).length === 0)) {
          // successfully validated
        } else if (typeof validated === "object" && Object.keys(validated).length > 0) {
          throw validationError(validated);
        } else {
          throw validationError({global: ["validation"]});
        }
      }
      const downloadedAssetRepo = downloadedAssetRepository();
      if (opts.field && opts.value) {
        return await downloadedAssetRepo.safeFindBy(opts.field, opts.value, opts.opts || {});
      } else if (opts.textSearch && DownloadedAssetTypeDef.textSearchFields.length > 0) {
        return await downloadedAssetRepo.find({ predicate: (obj) => DownloadedAssetTypeDef.textMatch(obj, opts.textSearch || "") });
      } else {
        return await downloadedAssetRepo.find(opts.opts || {});
      }
    });
  });
});
