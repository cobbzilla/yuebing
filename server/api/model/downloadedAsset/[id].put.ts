/* eslint-disable @typescript-eslint/no-unused-vars */
// DO NOT EDIT THIS FILE. AUTO-GENERATED BY mobiletto-orm-typedef-gen

import { H3Event } from "h3";
import { MobilettoOrmValidationErrors } from "mobiletto-orm-typedef";
import { DownloadedAssetType, DownloadedAssetTypeDef } from "yuebing-model";

export default defineEventHandler(async (event: H3Event) => {
  return await filterErrors(event, "downloadedAsset.create", async (event: H3Event) => {
    return await requireAdminAccountObject(event, "downloadedAsset.create", async (event: H3Event, session, account) => {
      const downloadedAssetRepo = downloadedAssetRepository();
      const id = event?.context?.params?.id;
      if (!id) throw notFound(downloadedAssetRepo.typeDef.idFieldName());
      const obj: DownloadedAssetType = await readBody(event);
      if (!obj) {
        throw badRequest();
      }
      if (DownloadedAssetTypeDef.id(obj) !== id) {
        const errs: MobilettoOrmValidationErrors = {};
        errs[downloadedAssetRepo.typeDef.idFieldName()] = ["mismatch"];
        throw validationError(errs);
      } else {
        return await downloadedAssetRepo.create(obj);
      }
    });
  });
});
