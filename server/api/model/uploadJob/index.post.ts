/* eslint-disable @typescript-eslint/no-unused-vars */
// DO NOT EDIT THIS FILE. AUTO-GENERATED BY mobiletto-orm-typedef-gen

import { H3Event } from "h3";
import { UploadJobTypeDef } from "yuebing-model";
import { MobilettoOrmFindApiOpts } from "/storeHelper.ts";

export default defineEventHandler(async (event: H3Event) => {
  return await filterErrors(event, "uploadJob.search", async (event: H3Event) => {
    return await requireAdminAccountObject(event, "uploadJob.search", async (event: H3Event, session, account) => {
      const opts: MobilettoOrmFindApiOpts = (await readBody(event)) || {};
      if (typeof UploadJobTypeDef.apiConfig.search?.validate === "function") {
        const validated = await UploadJobTypeDef.apiConfig.search.validate(account, null, opts);
        if (validated === true || (typeof validated === "object" && Object.keys(validated).length === 0)) {
          // successfully validated
        } else if (typeof validated === "object" && Object.keys(validated).length > 0) {
          throw validationError(validated);
        } else {
          throw validationError({global: ["validation"]});
        }
      }
      const uploadJobRepo = uploadJobRepository();
      if (uploadJobRepo.initialize) await uploadJobRepo.initialize();
      if (opts.field && opts.value) {
        return await uploadJobRepo.safeFindBy(opts.field, opts.value, opts.opts || {});
      } else if (opts.textSearch && UploadJobTypeDef.textSearchFields.length > 0) {
        return await uploadJobRepo.find({ predicate: (obj) => UploadJobTypeDef.textMatch(obj, opts.textSearch || "") });
      } else {
        return await uploadJobRepo.find(opts.opts || {});
      }
    });
  });
});
