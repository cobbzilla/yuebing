/* eslint-disable @typescript-eslint/no-unused-vars */
// DO NOT EDIT THIS FILE. AUTO-GENERATED BY mobiletto-orm-typedef-gen

import { H3Event } from "h3";
import { MediaProfileTypeDef } from "yuebing-model";
import { MobilettoOrmFindApiOpts } from "/storeHelper.ts";

export default defineEventHandler(async (event: H3Event) => {
  return await filterErrors(event, "mediaProfile.search", async (event: H3Event) => {
    return await requireAdminAccountObject(event, "mediaProfile.search", async (event: H3Event, session, account) => {
      const opts: MobilettoOrmFindApiOpts = (await readBody(event)) || {};
      if (typeof MediaProfileTypeDef.apiConfig.search?.validate === "function") {
        const validated = await MediaProfileTypeDef.apiConfig.search.validate(account, null, opts);
        if (validated === true || (typeof validated === "object" && Object.keys(validated).length === 0)) {
          // successfully validated
        } else if (typeof validated === "object" && Object.keys(validated).length > 0) {
          throw validationError(validated);
        } else {
          throw validationError({global: ["validation"]});
        }
      }
      const mediaProfileRepo = mediaProfileRepository();
      if (opts.field && opts.value) {
        return await mediaProfileRepo.safeFindBy(opts.field, opts.value, opts.opts || {});
      } else if (opts.textSearch && MediaProfileTypeDef.textSearchFields.length > 0) {
        return await mediaProfileRepo.find({ predicate: (obj) => MediaProfileTypeDef.textMatch(obj, opts.textSearch || "") });
      } else {
        return await mediaProfileRepo.find(opts.opts || {});
      }
    });
  });
});
