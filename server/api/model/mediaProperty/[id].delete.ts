/* eslint-disable camelcase,@typescript-eslint/no-unused-vars */
// DO NOT EDIT THIS FILE. AUTO-GENERATED BY mobiletto-orm-typedef-gen

import { H3Event, H3Error } from "h3";
import { MobilettoOrmSyncError } from "mobiletto-orm-typedef";

export default defineEventHandler(async (event: H3Event) => {
  return await filterErrors(event, "mediaProperty.delete", async (event: H3Event) => {
    return await requireAdminAccountObject(event, "mediaProperty.delete", async (event: H3Event, session, account) => {
      const mediaPropertyRepo = mediaPropertyRepository();
      const id = event?.context?.params?.id;
      if (!id) throw notFound(mediaPropertyRepo.typeDef.idFieldName());
      try {
        const mediaProperty_by_id = await mediaPropertyRepo.safeFindById(id);
        if (mediaProperty_by_id) {

          return await mediaPropertyRepo.remove(mediaProperty_by_id);
        }
      } catch (e) {
        if (e instanceof H3Error) throw e;
        if (e instanceof MobilettoOrmSyncError) {
          logger.warn(`mediaProperty.delete: sync error: ${e}`);
          throw conflict();
        }
        logger.error(`mediaProperty.delete: unexpected error: ${e}`);
        throw serverError();
      }
      throw notFound(id);
    });
  });
});
