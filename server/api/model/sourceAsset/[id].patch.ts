/* eslint-disable @typescript-eslint/no-unused-vars */
// DO NOT EDIT THIS FILE. AUTO-GENERATED BY mobiletto-orm-typedef-gen

import { H3Event } from "h3";
import { MobilettoOrmNotFoundError, MobilettoOrmValidationErrors } from "mobiletto-orm-typedef";
import { SourceAssetType, SourceAssetTypeDef } from "yuebing-model";

export default defineEventHandler(async (event: H3Event) => {
  return await filterErrors(event, "sourceAsset.update", async (event: H3Event) => {
    return await requireAdminAccountObject(event, "sourceAsset.update", async (event: H3Event, session, account) => {
      const sourceAssetRepo = sourceAssetRepository();
      const id = event?.context?.params?.id;
      if (!id) throw notFound(sourceAssetRepo.typeDef.idFieldName());
      const obj: SourceAssetType = await readBody(event);
      if (!obj) {
        throw badRequest();
      }
      if (SourceAssetTypeDef.id(obj) !== id) {
        const errs: MobilettoOrmValidationErrors = {};
        errs[sourceAssetRepo.typeDef.idFieldName()] = ["mismatch"];
        throw validationError(errs);
      } else {

        return await sourceAssetRepo.update(obj);
      }
    });
  });
});
