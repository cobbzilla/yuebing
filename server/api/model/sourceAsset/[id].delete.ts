/* eslint-disable camelcase,@typescript-eslint/no-unused-vars */
// DO NOT EDIT THIS FILE. AUTO-GENERATED BY mobiletto-orm-typedef-gen

import { H3Event, H3Error } from "h3";
import { MobilettoOrmSyncError } from "mobiletto-orm-typedef";

export default defineEventHandler(async (event: H3Event) => {
  return await filterErrors(event, "sourceAsset.delete", async (event: H3Event) => {
    return await requireAdminAccountObject(event, "sourceAsset.delete", async (event: H3Event, session, account) => {
      const sourceAssetRepo = sourceAssetRepository();
      if (sourceAssetRepo.initialize) await sourceAssetRepo.initialize();
      const id = event?.context?.params?.id;
      if (!id) throw notFound(sourceAssetRepo.typeDef.idFieldName());
      try {
        const sourceAsset_by_id = await sourceAssetRepo.safeFindById(id);
        if (sourceAsset_by_id) {

          return await sourceAssetRepo.remove(sourceAsset_by_id);
        }
      } catch (e) {
        if (e instanceof H3Error) throw e;
        if (e instanceof MobilettoOrmSyncError) {
          logger.warn(`sourceAsset.delete: sync error: ${e}`);
          throw conflict();
        }
        logger.error(`sourceAsset.delete: unexpected error: ${e}`);
        throw serverError();
      }
      throw notFound(id);
    });
  });
});
