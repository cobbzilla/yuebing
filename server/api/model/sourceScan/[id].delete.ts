/* eslint-disable camelcase,@typescript-eslint/no-unused-vars */
// DO NOT EDIT THIS FILE. AUTO-GENERATED BY mobiletto-orm-typedef-gen

import { H3Event, H3Error } from "h3";
import { MobilettoOrmSyncError } from "mobiletto-orm-typedef";

export default defineEventHandler(async (event: H3Event) => {
  return await filterErrors(event, "sourceScan.delete", async (event: H3Event) => {
    return await requireAdminAccountObject(event, "sourceScan.delete", async (event: H3Event, session, account) => {
      const sourceScanRepo = sourceScanRepository();
      const id = event?.context?.params?.id;
      if (!id) throw notFound(sourceScanRepo.typeDef.idFieldName());
      try {
        const sourceScan_by_id = await sourceScanRepo.safeFindById(id);
        if (sourceScan_by_id) {

          return await sourceScanRepo.remove(sourceScan_by_id);
        }
      } catch (e) {
        if (e instanceof H3Error) throw e;
        if (e instanceof MobilettoOrmSyncError) {
          logger.warn(`sourceScan.delete: sync error: ${e}`);
          throw conflict();
        }
        logger.error(`sourceScan.delete: unexpected error: ${e}`);
        throw serverError();
      }
      throw notFound(id);
    });
  });
});
