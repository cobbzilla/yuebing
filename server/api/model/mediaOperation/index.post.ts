/* eslint-disable @typescript-eslint/no-unused-vars */
// DO NOT EDIT THIS FILE. AUTO-GENERATED BY mobiletto-orm-typedef-gen

import { H3Event } from "h3";
import { MediaOperationTypeDef } from "yuebing-model";
import { MobilettoOrmFindApiOpts } from "/storeHelper.ts";

export default defineEventHandler(async (event: H3Event) => {
  return await filterErrors(event, "mediaOperation.search", async (event: H3Event) => {
    return await requireAdminAccountObject(event, "mediaOperation.search", async (event: H3Event, session, account) => {
      const opts: MobilettoOrmFindApiOpts = (await readBody(event)) || {};
      if (typeof MediaOperationTypeDef.apiConfig.search?.validate === "function") {
        const validated = await MediaOperationTypeDef.apiConfig.search.validate(account, null, opts);
        if (validated === true || (typeof validated === "object" && Object.keys(validated).length === 0)) {
          // successfully validated
        } else if (typeof validated === "object" && Object.keys(validated).length > 0) {
          throw validationError(validated);
        } else {
          throw validationError({global: ["validation"]});
        }
      }
      const mediaOperationRepo = mediaOperationRepository();
      if (opts.field && opts.value) {
        return await mediaOperationRepo.safeFindBy(opts.field, opts.value, opts.opts || {});
      } else if (opts.textSearch && MediaOperationTypeDef.textSearchFields.length > 0) {
        return await mediaOperationRepo.find({ predicate: (obj) => MediaOperationTypeDef.textMatch(obj, opts.textSearch || "") });
      } else {
        return await mediaOperationRepo.find(opts.opts || {});
      }
    });
  });
});
