/* eslint-disable camelcase,@typescript-eslint/no-unused-vars */
// DO NOT EDIT THIS FILE. AUTO-GENERATED BY mobiletto-orm-typedef-gen

import { H3Event, H3Error } from "h3";
import { MobilettoOrmSyncError } from "mobiletto-orm-typedef";

export default defineEventHandler(async (event: H3Event) => {
  return await filterErrors(event, "mediaOperation.delete", async (event: H3Event) => {
    return await requireAdminAccountObject(event, "mediaOperation.delete", async (event: H3Event, session, account) => {
      const mediaOperationRepo = mediaOperationRepository();
      const id = event?.context?.params?.id;
      if (!id) throw notFound(mediaOperationRepo.typeDef.idFieldName());
      try {
        const mediaOperation_by_id = await mediaOperationRepo.safeFindById(id);
        if (mediaOperation_by_id) {

          return await mediaOperationRepo.remove(mediaOperation_by_id);
        }
      } catch (e) {
        if (e instanceof H3Error) throw e;
        if (e instanceof MobilettoOrmSyncError) {
          logger.warn(`mediaOperation.delete: sync error: ${e}`);
          throw conflict();
        }
        logger.error(`mediaOperation.delete: unexpected error: ${e}`);
        throw serverError();
      }
      throw notFound(id);
    });
  });
});
