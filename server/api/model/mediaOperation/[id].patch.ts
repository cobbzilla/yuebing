/* eslint-disable @typescript-eslint/no-unused-vars */
// DO NOT EDIT THIS FILE. AUTO-GENERATED BY mobiletto-orm-typedef-gen

import { H3Event } from "h3";
import { MobilettoOrmNotFoundError, MobilettoOrmValidationErrors } from "mobiletto-orm-typedef";
import { MediaOperationType, MediaOperationTypeDef } from "yuebing-model";

export default defineEventHandler(async (event: H3Event) => {
  return await filterErrors(event, "mediaOperation.update", async (event: H3Event) => {
    return await requireAdminAccountObject(event, "mediaOperation.update", async (event: H3Event, session, account) => {
      const mediaOperationRepo = mediaOperationRepository();
      const id = event?.context?.params?.id;
      if (!id) throw notFound(mediaOperationRepo.typeDef.idFieldName());
      const obj: MediaOperationType = await readBody(event);
      if (!obj) {
        throw badRequest();
      }
      if (MediaOperationTypeDef.id(obj) !== id) {
        const errs: MobilettoOrmValidationErrors = {};
        errs[mediaOperationRepo.typeDef.idFieldName()] = ["mismatch"];
        throw validationError(errs);
      } else {

        return await mediaOperationRepo.update(obj);
      }
    });
  });
});
