// DO NOT EDIT THIS FILE. AUTO-GENERATED BY mobiletto-orm-typedef-gen

import { Ref } from "vue";
import { MobilettoOrmObject, MobilettoOrmPurgeResults, MobilettoOrmValidationErrors } from "mobiletto-orm-typedef";
import { ProfileJobType, ProfileJobTypeDef } from "yuebing-model";
import * as a from "~/utils/model/serviceHelper.js";

export const profileJobService = {
  searchProfileJob,
  findProfileJob,
  createProfileJob,
  updateProfileJob,
  deleteProfileJob,
};

function searchProfileJob(query?: MobilettoOrmFindApiOpts): Promise<ProfileJobType[]> {
  return $fetch("/api/model/profileJob", a.authPostJson(query)).then(a.handleJsonResponse<ProfileJobType[]>);
}

function findProfileJob(
  id: string,
  serverErrors: Ref<MobilettoOrmValidationErrors>,
): Promise<ProfileJobType> {
  return $fetch(`/api/model/profileJob/${id}`, a.authGet())
    .then(a.handleJsonResponse<ProfileJobType>)
    .catch(a.handleErrors(serverErrors)) as Promise<ProfileJobType>;
}

function createProfileJob(
  profileJob: ProfileJobType,
  serverErrors: Ref<MobilettoOrmValidationErrors>,
): Promise<ProfileJobType> {
  const id = ProfileJobTypeDef.id(profileJob);
  return $fetch(`/api/model/profileJob/${id}`, a.authPutJson(profileJob))
    .then(a.handleJsonResponse<ProfileJobType>)
    .catch(a.handleErrors(serverErrors)) as Promise<ProfileJobType>;
}

function updateProfileJob(
  profileJob: ProfileJobType,
  serverErrors: Ref<MobilettoOrmValidationErrors>,
): Promise<ProfileJobType> {
  const id = ProfileJobTypeDef.id(profileJob);
  return $fetch(`/api/model/profileJob/${id}`, a.authPatchJson(profileJob))
    .then(a.handleJsonResponse<ProfileJobType>)
    .catch(a.handleErrors(serverErrors)) as Promise<ProfileJobType>;
}

function deleteProfileJob(
  id: string,
  serverErrors: Ref<MobilettoOrmValidationErrors>,
  purge?: boolean
): Promise<MobilettoOrmObject | MobilettoOrmPurgeResults> {
  return $fetch(`/api/model/profileJob/${id}/${purge ? `?purge=${purge}` : ""}`, a.authDelete())
    .then(a.handleJsonResponse<MobilettoOrmObject | MobilettoOrmPurgeResults>)
    .catch(a.handleErrors(serverErrors)) as Promise<ProfileJobType>;
}
